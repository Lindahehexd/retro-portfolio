import { Box, Flex, Heading, VStack } from "@chakra-ui/react";

const AboutTitle = () => {
  return (
    <VStack>
    <Flex my={5} ml={6}>
      <Heading size="3xl" color="#7D7259"> 關於 About </Heading>
    </Flex>
    </VStack>
  );
};

export default AboutTitle;
